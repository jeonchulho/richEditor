import { ModalDialogBase } from "../popup/dialog-base";

export class TablePropsDialogComponent extends ModalDialogBase {
  constructor(root: HTMLElement, dialog: HTMLElement, backdrop: HTMLElement) {
    super(root, dialog, backdrop);
  }
}
