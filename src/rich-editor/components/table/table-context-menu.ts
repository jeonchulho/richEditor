import { ContextMenuBase } from "../popup/context-menu-base";

export class TableContextMenuComponent extends ContextMenuBase {
  constructor(root: HTMLElement, menu: HTMLElement) {
    super(root, menu);
  }
}
